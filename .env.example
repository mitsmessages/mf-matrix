# Gemini AI is optional and runs entirely in the user's browser with their own key.
# Never commit a real key. Only Vite-prefixed variables are exposed to the client.
VITE_AI_ENABLED=true
VITE_GEMINI_MODEL=gemini-2.5-flash
